import input from "$input/2016/25.txt";
import { register } from "$lib/aoc";

// TODO (really not a fan of the assembly-type problems)
export default function day25() {}

register(day25, "Clock Signal", 198);
